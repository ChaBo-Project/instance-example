<!--
Tier-1 prompt overrides. Only two sections are recognized — `query_rewrite_steps`
and `filter_extraction_steps` — and each overrides only the decision-making portion
of that prompt; the JSON output contract is always appended separately by the code
and can never be overridden here. Leave a section empty (or this whole file absent)
to use the framework's default steps.
-->

## query_rewrite_steps

## filter_extraction_steps
